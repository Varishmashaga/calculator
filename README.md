# calculator
A Java calculator program allows the user to perform basic arithmetic operations such as addition, subtraction, multiplication, and division by taking two numbers and an operator as input through the Scanner class.
